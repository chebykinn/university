

int main(){
	int num, x, m;	  
	m = 100;
	while(1)
    	for( x = 0; x < 100; x++ ) {
    		num = 1;
			num = ( m - (5 * x) / 9 );
			num = ( m - (x * num) / 2 / m);
			num = ( m - (x * num) / 4 / m);
			num = (x * num) / 2 / m;
			P3 = num;
    	}
}
